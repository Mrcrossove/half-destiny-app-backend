# Migration Skeletons

These files are initial migration skeletons for the standalone backend.

They are intentionally lightweight and grouped by schema domain:

- identity
- billing
- compliance
- growth

The current project does not yet include a migration runner. These files serve as the initial source of truth for incremental schema evolution after the bootstrap SQL.
